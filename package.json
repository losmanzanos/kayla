{
  "name": "mariposa-mental-wellness",
  "version": "1.0.0",
  "private": true,
  "description": "Mariposa Mental Wellness — static site with a TinaCMS-managed blog.",
  "type": "module",
  "scripts": {
    "blog": "node scripts/build-blog.mjs",
    "siteinfo": "node scripts/build-site.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "npm run blog && npm run siteinfo && npm run serve",
    "tina": "tinacms dev -c \"node scripts/serve.mjs\"",
    "build": "tinacms build && npm run blog && npm run siteinfo"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.5.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "tinacms": "^2.2.0"
  }
}
